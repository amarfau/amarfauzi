function findSame(arr){
    var text = []
    var arrs = arr 
    for (let i = 0; i < arr.length; i++) {
        text.push(arr.find((e) => arrs))
    }
    return text;
}
console.log(findSame(['cat', 'act', 'tcc']))